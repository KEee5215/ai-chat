## 目标

创建一个基于langchain的AI聊天机器人，并使用FastAPI作为后端,提供接口，并使用postgresql作为数据库。

## 技术栈

后端 (Server): Python FastAPI (异步 Web 框架)。
AI: 使用langchain , 调用openai兼容api, 构建agent核心
数据库: postgresql

## 步骤

1. 创建虚拟环境并安装依赖
2. 创建FastAPI项目
3. 创建数据库并初始化数据
4. 创建langchain核心
5. 创建接口
6. 测试接口
